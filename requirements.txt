# TensorFlow for TFLite conversion (compatible with tensorflow-addons)
tensorflow==2.14.0

# ONNX for model loading
onnx==1.14.1

# TensorFlow Probability (required by some TF operations)
tensorflow-probability==0.22.0w for TFLite conversion (compatible with tensorflow-addons)
tensorflow==2.14.0

# ONNX for model loading
onnx==1.14.1

# tf2onnx for converting ONNX -> TensorFlow (alternative to onnx-tf)
tf2onnx==1.16.1

# TensorFlow Probability (required by some TF operations)
tensorflow-probability==0.22.0


